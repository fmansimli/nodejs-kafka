export enum CollectionNames {
  TRIPES = "tripes",
  USERS = "users",
  CITIES = "cities",
  AREAS = "areas",
  COLLECTIONS = "collections",
  PLACES = "places",
  EVENTS = "events",
}

export enum Models {}
